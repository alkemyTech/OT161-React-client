import React from 'react';
import { Rings } from 'react-loader-spinner';

const Spinner = () => {
	return (
		<div>
			<Rings color='#214763' height={80} width={80} />
		</div>
	);
};

export default Spinner;
